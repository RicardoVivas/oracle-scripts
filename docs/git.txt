rcs 
cvs hooks
ms sourcesafe
svn
git full history, fast, merget, ligthweight branch

git init  : create .git directory
git add
git log
git show <hash_string>
git status

git clone cuscab@a:~/rep
git commit -a -m "" :  "a" means add
git remote show origin
git fetch
git differ master origin/master
git pull: fetch & merge

you can git differ remote 

git remote add cuscab@~/demo/myrep

scm.warwick.ac.uk

merge vs rebase : rebase is preferred. keep linear log

git branch

git log <file name>

git co -patch hash_string

git tag : list all


git remote show origin

git diff  master origin/master

git has two local master : master and origin master. Origin master track changes in remote repository.

git pull =  git fetch & merge

Add  repository from another PC
git remote add cuscax@webora: ~/demo/myrepo
git diff webora/master master

git merge

scm.elab.warwick.ac.uk

merge vs rebase (preferred)

git co <branch>

git merge new tag

//untrack a file that alrady tracked:
git rm --cached filename



